const registeruser = async (firstname, surname, email, number) =>{
    try {
        if(!firstname || !surname || !email || !phoneNumber || !password ){
            throw Error("All fields are required")
        }

        // whatsapp op
    } catch (error) {
        throw error
    }
}

module.exports = registeruser